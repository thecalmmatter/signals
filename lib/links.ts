// Shared external links used across marketing/app surfaces — single place
// to update if the URL ever changes, instead of hunting through every page
// that links out to it.

/** Substack newsletter — insights/commentary alongside the live signal
 *  feed. Pure cross-promotion (README §15): no subscriber-status gating or
 *  content sync with the app, just outbound links pointing readers there. */
export const SUBSTACK_URL = "https://smartalphas.substack.com/";
